# nothing needs to be added here. 
# It is standard practice in Python to leave __init__.py completely empty. 
# Its only job is to tell Python that the pipeline folder should be treated as an importable module (which allows you to run from pipeline.config import ...). 